{% if post.image %}
  <div class="banner" style="background-image: url('{{ Image(post.meta('image')).src }}');">
    {% if post.banner_text %}
      <span class="headline">{{ post.banner_text }}</span>
    {% endif %}
  </div>
{% endif %}
